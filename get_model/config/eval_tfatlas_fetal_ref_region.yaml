defaults:
  - base_ref_region_config
  - model/GETRegionFinetune@_here_
  - machine/pc
  - dataset/tfatlas_eval@dataset
  - _self_

assembly: "hg38"

training:
  save_ckpt_freq: 1
  epochs: 100
  warmup_epochs: 1
  accumulate_grad_batches: 1
  clip_grad: null
  use_fp16: false

quantitative_atac: false

optimizer:
  lr: 0.0001
  min_lr: 0.0001
  weight_decay: 0.05
  opt: "adamw"
  opt_eps: 1e-8
  opt_betas: [0.9, 0.95]

wandb:
  project_name: "debug"
  run_name: "debug"

finetune:
  checkpoint: "/home/xf2217/Projects/geneformer_esc/Astrocytes_natac/checkpoint-best.pth"
  model_prefix: "model."
  patterns_to_freeze: []
